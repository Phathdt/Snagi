= link_to "Index" , albums_path
= link_to "Edit", edit_album_path

- @pictures.each do |picture|
  %p= link_to image_tag(picture.image.url), picture_path(picture)
  %p= picture.title
  = link_to 'Destroy', picture, method: :delete, data: { confirm: 'Are you sure?' }