.wrapper
  = render "shared/wallpaper_profile", url: @user
  .main.main-raised
    .container
      .section.text-center
        %h2.title List User
        .team
          .row
            - @users.each do |user|
              = render "shared/user", user: user   